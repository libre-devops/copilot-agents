## What changed

<!-- One or two sentences. If an agent's behaviour changed, say how. -->

## Checks

- [ ] `just validate` passes (render drift gate plus lint)
- [ ] `rendered/` re-rendered and committed if a fragment or definition changed
- [ ] UK English, no em dashes or en dashes
- [ ] New or changed platform claims cite the Microsoft page and the date checked

## Instruction budget

<!-- Paste the render output so the spend is visible in review. -->

```
```

## Tenant testing

<!-- The gates prove the package is well formed. They cannot prove behaviour.
     If instructions changed, what did you run in a tenant and what did it do?
     Write "not tested" if you did not, so a reviewer knows. -->
